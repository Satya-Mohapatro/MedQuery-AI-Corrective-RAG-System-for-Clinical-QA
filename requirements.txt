# ============================================================
#  Medical CRAG System — Python Dependencies
#  Install: pip install -r requirements.txt
#  Python: 3.10+
# ============================================================

# -- LangChain Core --------------------------------------------
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-chroma>=0.1.0

# -- LangGraph (Agentic Pipeline) ----------------------------
langgraph>=0.1.0

# -- Groq LLM (Free, fast — get key at console.groq.com) ------
langchain-groq>=0.1.0
groq>=0.9.0

# -- HuggingFace Embeddings ONLY (for all-MiniLM-L6-v2) ------
sentence-transformers>=2.2.0
langchain-huggingface>=0.0.6

# -- Vector Store --------------------------------------------
chromadb>=0.5.0

# -- Web Search Fallback -------------------------------------
tavily-python>=0.3.0

# -- Component Splitters -------------------------------------
langchain-text-splitters>=0.2.0

# -- Document Processing -------------------------------------
pypdf>=3.0.0
# unstructured>=0.10.0   # Removed: Causes severe build errors and huge disk bloat on cloud

# -- Scientific Computing ------------------------------------
numpy>=1.24.0

# -- Visualization & Notebook --------------------------------
jupyter>=1.0.0
ipykernel>=6.0.0
matplotlib>=3.7.0
ipython>=8.0.0

# -- Streamlit (Phase 2 UI) ----------------------------------
streamlit>=1.35.0

# -- Observability --------------------------------------------
langsmith>=0.1.0

# -- Utilities -----------------------------------------------
python-dotenv>=1.0.0
pydantic>=2.0.0
httpx>=0.25.0
tenacity>=8.2.0

# ============================================================
#  QUICK INSTALL NOTES:
#
#  1. Create virtual environment:
#     python -m venv .venv && source .venv/bin/activate
#
#  2. Install all deps:
#     pip install -r requirements.txt
#
#  3. Set API keys (create .env file):
#     GROQ_API_KEY=your_key_here   (get free at console.groq.com)
#     TAVILY_API_KEY=your_key_here
#     LANGSMITH_API_KEY=your_key_here  (optional)
#
#  4. Run Jupyter notebook:
#     jupyter notebook Notebook/medical_crag.ipynb
#
#  5. Run Streamlit app:
#     streamlit run streamlit_app.py
# ============================================================
